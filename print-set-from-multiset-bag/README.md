# Print multiset(bag) from set
*The commonly stated problem definition is "print only the unique elements of a set." This is what it really means.*
>• **Set theory:** a `multiset, or bag`, is a set in which repeated items are allowed ("multiple instances") such as `{ a, a, b, c }`. A `set` has unique, non-repeated elements such as `{a, b, c}`.

## Solutions in C++, JavaScript

## JavaScript Solution 0: set-from-multiset.js
### nested for loops


...
