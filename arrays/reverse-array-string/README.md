# Reverse an array
>• C++ incrementing for loop followed by decrementing for loop : `O(n) + O(n) ~ O(n)`

>• JavaScript .map and .call with split, reverse, join

>• JavaScript recursion (slice and append)

>• JavaScript split, reverse, join

>• JavaScript decrementing for loop and concatenation
